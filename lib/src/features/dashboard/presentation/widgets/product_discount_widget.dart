import 'package:flutter/material.dart';

class ProductDiscountWidget extends StatelessWidget {
  const ProductDiscountWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
